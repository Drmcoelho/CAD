# STATUS — trilha de auditoria

Estado do canon por camada, na versão `CAD360-ApA-2026-07-01`. O que estiver ✅ passa no `npm run check`.

## Motor e doutrina
- ✅ `core/cad_core.js` — `POLICY` congelado, 292 asserções passando. K `<3,5 / 3,5–5,0 / ≥5,0`; bicarbonato pH-driven; Na 1,6/2,4; Δ/Δ `<1 / 1–2 / >2`; osm efetiva com Na medido.
- ✅ `canon/policy.json` — espelho em JSON, batendo com o core (verificado no portão).
- ✅ `core/abg_core.js` — motor geral de acid-base (não específico de CAD), 16 asserções passando. Reusa AG/AGc de `cad_core.js`; branch-order corrigido para direção de pH decidir o primário quando HCO₃/pCO₂ divergem em direções opostas (bug real, achado antes de escrever os casos de gasometria — ver CHANGELOG 2026-07-05).

## Banco de gasometrias (`content/gasometrias.json`, `painel/index.html`)
- ✅ **Completo: 100/100 casos** (G-01..G-100), 100 diagnósticos diferentes, verificados por `scripts/check_gasometrias.js` (Henderson-Hasselbalch, distúrbio primário, AG/AGc, hasDka). UI em `painel/index.html`, separada da série real.
- ✅ **Quiz por caso, 8 mcq + 3 V/F + 3 assertivas**: os 100 casos têm `quiz{mcq[8],vf[3],assertivas[3]}` — estrutura e índice de resposta correta verificados mecanicamente por `check_gasometrias.js` (assertivas recomputadas a partir de `verdades`, mcq[0] cruzado contra o título do caso). Render interativo em `painel/index.html` no mesmo padrão do provão de `app/index.html`.
- ✅ **Evolução do caso**: os 100 casos ganham `evolucao{pontos[1-3],escada[8],casosRelacionados}` — série temporal (P2/P3/P4 além do labs original, no espírito "filme, não foto"), cada ponto com pH recomputado por Henderson-Hasselbalch a partir de hco3/pco2, e uma escada de 8 passos socráticos que testam e avançam o raciocínio ao mesmo tempo, liberando "casos relacionados" só no último passo. Seção nova em `painel/index.html`, separada do Teste-se.
- ✅ **Spoiler eliminado da vinheta/pergunta**: as ~54 vinhetas/perguntas que citavam outro caso por id (ex.: "mesmo fator de risco de TEP de G-17") antes do reveal foram reescritas nos 100 casos — a comparação rica migrou para `casosRelacionados`, liberada só no fim da escada. `check_gasometrias.js` barra qualquer menção a "G-NN" em vinheta/pergunta/escada.
- ✅ **100 cards clicáveis com feedback visível**: `selGaso()` rola o painel de detalhe para a viewport ao selecionar um card (antes, clicar num card do topo atualizava o detalhe fora da tela, sem feedback perceptível).
- ✅ **Sortear caso, sem spoiler**: botão "Sortear caso" escolhe um caso aleatório (respeitando o filtro de categoria ativo, sem repetir o sorteio imediatamente anterior) e entra em modo cego — título, categoria e badge "imita CAD" ficam ocultos, tanto no painel de detalhe quanto no card destacado na grade, até o aluno clicar em "revelar gabarito". Clique direto num card da lista continua mostrando título/categoria de cara (navegação normal, sem modo cego).
- ✅ **Ordem do reveal corrigida**: achado real em uso — o botão liberava a explicação narrativa completa (classificação/diagnóstico/raciocínio) ANTES do Teste-se e da Evolução, entregando a resposta da própria MCQ "qual o diagnóstico" antes de ela ser feita. Reordenado: Teste-se → Evolução do caso → Gabarito completo (debrief), mesmo clique único, botão renomeado para "praticar este caso".
- ✅ **"Evolução do caso" corrigida — bug real, não só percepção**: os botões "iniciar evolução"/"próximo passo"/"revelar casos relacionados" usavam `onclick="..."` de aspas duplas com `JSON.stringify(c.id)` embutido (também aspas duplas) — colisão que quebra o atributo e lança `SyntaxError` no clique real, silenciosamente (mesma classe do bug já corrigido nos handlers do quiz, que ficou de fora desta seção). Corrigido para aspas simples. Adicionado `gasoEvoScrollTo()` (rola o passo novo/casos relacionados para a viewport). **Nota de processo**: a varredura anterior usava `page.evaluate()` direto, que não reproduz esse bug — só clique físico real expõe. "Casos que confundem com este" também deixou de entregar título, só o id.
- ✅ **Spoiler cruzado eliminado de todo o texto revelável, não só vinheta/pergunta/escada**: achado real em uso — o portão anterior só varria vinheta/pergunta/escada, mas `gabarito.raciocinio/detalheChave/armadilha/diagnostico/classificacao/conteudoComplementar` e todo o `quiz` (mcq/vf/assertivas) citavam outro caso por id em 757 pontos do banco (97/100 casos), a maioria revelada de cara no clique único de "praticar este caso" — sem esperar o fim da escada de evolução, sem passar por `casosRelacionados` (o único campo com permissão de citar outro caso). `check_gasometrias.js` endurecido pra varrer todo o texto revelável (novo bloco "código misturado com linguagem natural" também barra nomes de campo JSON tipo `detalheChave` vazando pra prosa, chamadas de função cruas e ids internos entre aspas). Reescritos os ~343 campos com spoiler real e ~46 com vazamento de código, em 10 lotes paralelos de ~10 casos, preservando o raciocínio clínico (generalizado sem citar o id, ou promovido para `casosRelacionados` quando o comparativo era genuinamente valioso). Verificado: portão 100/100 sem falha, `npm run ci` verde, varredura Playwright estrutural nos 100 casos + interação completa (clique físico real) em 20 casos, zero erro de console.
- ✅ **Código misturado com linguagem natural corrigido também em `content/fisiopatologia.json` e `content/questions.json`** (Provão, `app/index.html`): explicações citavam nomes de função crus (`hasDka()`, `classifyDkaProfile()`, `potassiumPlan()`...), paths tipo `computed.hasDka`/`matches.0.id`, ids internos entre aspas (`'cad-hhs'`, `'pre-cad'`...) e `AND`/`OR` em caixa-alta como pseudo-operador — tudo reescrito em português natural, sem tocar o bloco `computed[]` (metadata de verificação mecânica, nunca renderizada). Uma pergunta de `questions.json` também tinha um aparte que respondia a si mesma antes da hora ("ainda acima do limiar de 250? não — abaixo"); removido.
- ✅ **Atlas (Projeto 1/2/3) ganhou referência para autoavaliação**: os três projetos "o aluno constrói" (galeria de fenótipos, ler-um-filme, leis de memória) não tinham nenhum gabarito/exemplo pra comparar. Adicionado um exemplo resolvido por projeto (números do Projeto 2 recomputados via `cad_core`/`abg_core`), atrás de um botão "ver referência" no mesmo padrão de reveal (`mkReveal`) já usado no resto do app.

## Tratado (`tratado/index.html`), Perfis (`perfis/index.html`), Tutor/Marco
- ✅ **Vazamento de código-em-prosa corrigido em `content/profiles.json`, `perfis/index.html`, `tratado/index.html`**: `hasDka()`/`isResolvedDka()` crus na leitura de cada um dos 9 perfis sintéticos, reescritos em português natural.
- ✅ **Tutor (`perfis/`) e Marco (`tratado/`) — bug real de UI**: as duas cópias independentes do widget renderizavam nome de função cru (`hasDka() true`, `isResolvedDka() false`) como rótulo na tela; corrigido para "critério de CAD fechado"/"resolução" nos dois.
- ✅ **K wired ao classificador**: o campo de potássio do Tutor/Marco era capturado e nunca usado por `classifyDkaProfile()` — decorativo desde sempre. `potassiumPlan()` agora é chamada com `input.kMmolL`; `computed.potassiumPlan` exposto e nota de conduta gerada nas bandas `hold` (K&lt;3,5) e `>=5.0`. Doc-comment que afirmava "lista ranqueada" corrigido (é pilha em ordem fixa de eixo clínico). 4 testes novos em `core/cad_core.test.js`.
- ✅ **`content/atlas.json` (banco B-1..B-6)**: 6 exercícios expandidos de "Lição" de 1-2 frases para explicação de mecanismo (300-800+ caracteres), sem número novo.
- ✅ **20 lâminas do tratado (seção 13), 636 itens de MCQ/V-F reescritos**: achado real — a maioria era rasa ("Gabarito: A. Síntese da lâmina.", ~344 V/F sem nenhuma justificativa). Reescritos em 10 lotes paralelos (2 lâminas por lote), ancorados nos números canônicos já estabelecidos, sem inverter veredito. Merge por linha exata (`class="lam-nota"`). Verificado: zero vazamento de código, zero duplicata de texto, `npm run ci` verde, Playwright sem erro de console.

## Autoverificação socrática intercalada (`tratado/index.html`, seções 1/3/7)
- ✅ **16 checkpoints `<details class="q">` intercalados**: achado real — o tratado só tinha uma pergunta socrática por seção, ao final, apontando para o banco externo (§11) em vez de resolver ali. Adicionado um checkpoint após cada subseção (6 na §1 Fisiopatologia, 6 na §3 ácido-base, 4 na §7 loops de conduta), reveal ao clique no mesmo componente visual do banco de questões, resposta resolvida no próprio bloco. As 3 perguntas de síntese que já existiam também passaram a resolver inline em vez de só apontar pro §11.

## Navegação entre seções
- ✅ **`nav.crumbs`** em `app/`, `tratado/`, `perfis/`, `painel/` — link para `../` (Início) + as outras 3 seções, seção atual marcada. Antes só `perfis/` tinha um link de volta (e só para `tratado/`, não para as 3). `render_smoke.js` confere a estrutura das 4 (Início + 3 seções + atual) em `checkCrumbs()`.

## Banco de fisiopatologia/mecanismo (`content/fisiopatologia.json`, `app/index.html` aba Provão)
- ✅ **32 questões** (lote 1: 16 + lote 2: 9 + lote 3: 7), ancoradas a `cad_core.js`/`abg_core.js` via array `"computed"`. Lote 3 (2026-07-21, +4 mcq/+2 vf/+1 diss) fecha a cobertura das **fórmulas de compensação** antes órfãs de âncora: `winter`/`expectedPco2MetabolicAcidosis` (régua para detectar 2º distúrbio), `expectedPco2MetabolicAlkalosis` (teto da hipoventilação vs drive hipóxico), `expectedHco3RespAlkalosis` agudo×crônico (0,2 vs 0,4, o trabalho renal), e o `deltaRatio` numérico cru. 32/32 batendo no portão. Cada afirmação de mecanismo é recomputada de verdade, não só prosa. Lote 2 fecha `correctedAnionGap()` (albumina), o limiar de HHS (300 do motor vs 320 do critério formal), `isResolvedDka()` isolada, e mais 2 dos 8 perfis de `classifyDkaProfile()` (`pre-cad`, `alcoolica-jejum`). `scripts/check_fisiopatologia.js` recalcula 29 âncoras do banco **e** as 8 âncoras de conduta (K/insulina) em `content/questions.json` (mesmo campo `"computed"`), fechando o gap entre "conduta" (mais macia, guideline) e as duas funções que já a codificam deterministicamente (`potassiumPlan`/`insulinPlan`). UI em `app/index.html` (`#fisioQuiz`), reaproveitando 100% do render/placar já existente.
- ✅ **Aquecimento socrático antes do banco avaliativo (`content/warmup.json`, `#warmup`)**: achado real — o Provão era puramente avaliativo desde o primeiro clique, sem nenhuma camada "decida antes de revelar". 6 perguntas sem placar, cada uma revisitando uma armadilha/mecanismo já central no projeto (insulina vs bicarbonato, paradoxo do K, Na medido na osm efetiva, glicemia ≠ resolução, Δ/Δ 0,93, insulina subordinada ao K), reaproveitando 100% o componente `mkReveal` já existente. `build_app.js`/`render_smoke.js` cobrindo o bloco novo.

## Locale decimal e fonte renderizada da auditoria (2026-07-21)
- ✅ **Locale unificado para vírgula (pt-BR)** — backlog P2 fechado. `app/` ganha `fx()` e `r()` passa por ele; `.toFixed()` visíveis (eixos do canvas, tabelas Winter/isohídrica, saídas Davenport/cloro) roteados por `fx()`; `painel/` ajusta o próprio `r()`; Tutores de `perfis/`/`tratado/` formatam os números do core por vírgula; eixos SVG do gráfico de pH do `tratado/` idem. `num()` do `app/` tolera vírgula **ou** ponto na entrada. Deep-link `#calcular?…` permanece em ponto (URL, não display) — sem regressão no `render_smoke.js`. Bibliotecas empacotadas intocadas.
- ✅ **`docs/auditoria.docx` aposentado; fonte renderizada no lugar.** O `.docx` era o único artefato editado à mão e seus números driftaram 5× (erratum 01–05). Agora a **fonte de verdade** é `docs/auditoria.src.html` — fragmento semântico onde cada número clínico é um **token `{{…}}`** resolvido de `canon/policy.json` + `core/cad_core.js` (a mesma fonte única de app/tratado/painel/pranchas). `scripts/build_auditoria.js` resolve os tokens e renderiza `docs/auditoria.html` (auto-contido, determinístico) e `docs/auditoria.pdf` (Chromium/Playwright — o motor que funciona aqui; pandoc/`soffice` indisponíveis). `npm run build:auditoria` regenera; `build_auditoria.js --check` (em `npm run check`/`ci`) barra token não resolvido, `.html` fora de sincronia com fonte+canon, número canônico ausente ou forma obsoleta no render. Semente extraída fielmente do `.docx` (338 parágrafos, headings/tabelas/listas), sem retranscrição. `.docx` removido do repo (recuperável via git). Regressões comprovadas (output adulterado e token obsoleto barrados).

## Pranchas — lote 1 (M01–M06 + EX01–EX02)
- ✅ M04 calculadoras — bandas Δ/Δ corrigidas, osm separada (tonicidade × osm-gap), exemplo glicose 320 (evita o fator 2,4).
- ✅ EX01 Δ/Δ 0,71 — resolve o conflito entre pranchas ("rótulo antigo: pura" → "corrigido: hiperclorêmica").
- ✅ **M06 insulina — furo do K fechado.** A "regra de plantão" agora usa `K < 3,5 / 3,5–5,0 / ≥5,0`, com a nota "o antigo 5,5 não é doutrina deste projeto". Documentado em `pranchas/revisao-m2-m4-m6.png`. Colisão de layout resolvida.

## Pranchas — lote 2 (M07–M12 + EX03–EX04)
- ✅ M07–M12 e EX03 — canon-alinhados (K, bicarbonato pH<7,0 não-rotina, jejum/transição, gasometria-filme, compensação lenta, condutas por perfil).
- ✅ **EX04 (CAD+HHS) — corrigido em JSON + SVG + PNG.** A osm efetiva usava **Na corrigido** (`2·142 + 600/18 = 317,3`), que dupla-conta a glicose. Agora usa **Na medido**: `2·130 + 600/18 = 293,3`. O template do gerador virou **data-driven** (lê `na`/`effective_osm` do JSON, não pode mais divergir), e a interpretação foi corrigida: 293 ainda **não** cruza o limiar HHS (>320); o Na corrigido 142 sinaliza o déficit de água livre emergente. O portão de CI (passo [5]) enforça isso para todos os exercícios.

## Fase 1 — hotfix clínico (auditoria externa, 01/07)
- ✅ A1: "0,93 quase pura" → "limítrofe, logo abaixo de 1".
- ✅ Operadores de K → `≥` (reinício `K ≥ 3,5`; banda inicial `K ≥ 5,0`). Render da aba Referência revisado.
- ✅ Bicarbonato: frase seca → "decisão de terapia intensiva conforme gravidade/protocolo local/risco".
- ✅ hrefs do app corrigidos para o layout do repo (`../tratado/`, `../painel/`, `../docs/`).
- ✅ `core/fixtures.json` + `fixtures.test.js` (12 asserções: Δ/Δ 0,71/0,93/1,0/2,1, osm, K).
- ✅ `scripts/test_links.js` no CI.
- Adjudicação completa da auditoria externa em `ROADMAP.md` §1. Fases 3–5 abertas lá.

## Reconciliações de doutrina
- **Bicarbonato.** `core.js` usa `considerBelowPh: 7.0` (literal do consenso 2024). `canon/policy.json` e o `app/` acrescentam `noBenefitAbovePh: 6.9` (fronteira de evidência: sem benefício acima de 6,9; abaixo, nem dado nem consenso). Ambos convivem — 7,0 é o número que o consenso escreve; 6,9 é a fronteira de evidência. Se quiser um número único no core, adicionar o campo `noBenefitAbovePh` ao `POLICY`.

## Limpeza final (auditoria de robustez, encerramento)
- ✅ **Markdown cru na seção 14 do tratado**: `**negrito**`, pseudo-tabela em pipes e `---` renderizavam como texto literal (sem parser de Markdown carregado). Convertidos para `<strong>`, `<table>` real (mesmo CSS `.tbl` do resto do tratado), `<code>`, `<hr>` e `<ul><li>`.
- ✅ **Duplicidade lâmina 3 × lâmina 13**: reauditado após a reescrita dos 636 itens — zero frase compartilhada entre as 31 notas de cada lâmina; tocam "osm efetiva" por ângulos diferentes (erro de fórmula vs. limiar de HHS), não é repetição.
- ✅ **Autorreferência a CLAUDE.md**: reduzida a 1 menção, na seção 14 (planejamento do próprio roadmap do tratado) — contextualmente apropriada, não vaza para gabarito clínico.
- ✅ **`content/profiles.json#_meta.canon`**: confirmado sem regressão (operadores `≥`/`→`/minúsculo, já corrigidos na Task de vazamento de código).

## Aberto / próximo
- [x] ~~Regenerar EX04 (SVG/PNG)~~ — feito; template agora data-driven.
- [x] ~~Sincronizar `docs/auditoria`~~ — reconciliado em `docs/auditoria-erratum.md` (Δ/Δ 0,93 = limítrofe, não "quase pura"; bicarbonato consenso pH<7,0 + fronteira ≤6,9; compensação dos 4 distúrbios). **2026-07-05**: a alegação anterior de que os pontos 1–2 "já estavam aplicados no `.docx`-fonte" era falsa (achado na auditoria própria pós-PR#15) — o `.docx` ainda tinha "quase pura" e não tinha "6,9". Corrigido de fato, mais 2 achados novos (osm efetiva do B-6 dupla-contando a glicose — mesmo furo do EX04; operador `> 3,5` em vez de `≥ 3,5` no Apêndice A). `docs/auditoria.pdf` regenerado — `soffice` está instalado neste ambiente mas sua conversão headless está quebrada (falha até em `.txt` trivial); fallback usado foi `pandoc` (docx→HTML) + Chromium headless (HTML→PDF), documentado em `docs/auditoria-erratum.md` como provisório até um LibreOffice funcional regenerar no padrão nativo.
- [x] ~~Fazer o `app/index.html` expor seu `CANON` como bloco `<script type="application/json">`~~ — feito; bloco `id="canon"` (espelho do core) + self-check em runtime. `check_consistency.js` passo [7] faz deep-equal contra `core.POLICY` (valida valor-a-valor, não mais só token).
- [ ] Próximos lotes de pranchas: auditar contra o mesmo `check_consistency.js` antes de commitar.
- [x] ~~Auditoria própria pós-Fase 7~~ — **2026-07-05**: os mesmos 2 furos do `.docx` (osm efetiva com Na corrigido; operador `>` em vez de `≥` no reinício de K) estavam **ao vivo no app publicado**, em `content/atlas.json` (banco B-6 + leis[3]), `content/questions.json` (Q2/Q3) e um exercício hardcoded dentro do próprio `app/index.html` (calculadora de Osmolaridade efetiva — exceção não documentada à regra de fonte única). Todos corrigidos; ver CHANGELOG 2026-07-05.
- [x] ~~Portão automático para a prosa livre de `atlas.banco`/`atlas.leis`/`questions.exp`~~ — **2026-07-05**: `scripts/check_content_text.js`, em `npm run check`/`ci`. Cruzamento global no corpus (não por string isolada) para o furo de osm efetiva, já que o furo real era cruzado entre exercícios (B-5→B-6); testado reintroduzindo os dois furos reais artificialmente antes de confirmar zero falso positivo no estado atual.
- [x] ~~Unificar o exercício hardcoded da calculadora "Osmolaridade efetiva" com o banco B-6 do Atlas~~ — **2026-07-05**: na verdade eram **5** calculadoras duplicadas (AG/AGc/Δ/Δ/Na corrigido/osm), não só uma. `ATLAS` passa a ser parseado antes de `CALCS`; `atlasEx('B-N')` busca o exercício certo do banco em vez de repetir o texto. `check_content_text.js` barra recorrência da duplicação; `render_smoke.js` confirma paridade em runtime.
