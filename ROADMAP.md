# ROADMAP — construção do repositório

Consolida a auditoria externa (`docs/auditoria_externa.pdf`) com o que já está construído. Regra de ouro adotada: **número clínico nasce em `canon/policy.json`+`core/`; cálculo no core; conteúdo pedagógico em JSON; HTML/DOCX/SVG/PNG/PDF são saídas renderizadas.**

## 1. Adjudicação da auditoria externa

**Onde ela acerta e já foi aplicado (Fase 1 — hotfix clínico):**
- `A1 "0,93 quase pura"` → `"limítrofe, logo abaixo de 1; não chamar de pura sem olhar tendência/Cl"`. Aplicado.
- Operadores de K padronizados para `≥`: reinício `K ≥ 3,5`, banda inicial `K ≥ 5,0` (elimina a zona cinzenta em K=3,5 e K=5,0 contra o core). Aplicado no HTML.
- Bicarbonato: a frase seca "abaixo disso não há dado nem consenso" virou "não há RCT que guie: é decisão de terapia intensiva conforme gravidade, protocolo local e risco (hipocalemia, CO₂/SNC)". Aplicado.

**Onde ela acerta e vira trabalho (adotado no repo):**
- Fixtures clínicas (`core/fixtures.json` + `fixtures.test.js`): Δ/Δ 0,71 / 0,93 / 1,0 / 2,1 + osm 287,8 + CAD+HHS 293,3, recomputadas pelo core. Feito.
- Teste de links (`scripts/test_links.js`): quebra o build se href do app não existir. Feito.
- Externalizar casos/questões para `content/*.json` (hoje embutidos no HTML/geradores). **Aberto — Fase 3.**
- Fallback textual + tabela equivalente para os 2 canvas (acessibilidade). **Aberto — Fase 4.**
- Changelog clínico versionado (fonte/data/impacto por mudança de limiar). **Aberto — Fase 5.**

**Onde a recomendação #1 dela já era o núcleo do repo (convergência, não novidade):**
- "Promover core/policy.json a fonte e barrar drift" = exatamente `canon/policy.json` + `scripts/check_consistency.js` + CI, já entregues na rodada anterior. A auditoria valida a arquitetura; não a antecede.

**Onde ela erra (artefato de método — não acatar como deficiência):**
- Inventário "Canvas 0 / Inputs 0 / Botões 0" e Apêndice A "CANON não localizado": **falso**. O HTML entregue tem **2 canvas**, **18 refs de input/JS** e `const CANON=` localizável (confirmado por grep e por render). É limitação de *parse estático* sobre um app gerado em runtime (provável cache velho: `/.cache/...`). O confronto HTML×core da §8, portanto, foi inferido do texto renderizado, não do objeto CANON real.

**O que ela deixou passar (minha auditoria pegou):**
- EX04 (CAD+HHS): a osm efetiva usava **Na corrigido** (`2·142+600/18=317,3`), dupla-contando a glicose. A externa validou o lote2 como "gerado" sem ver o furo. Corrigido em JSON+SVG+PNG e o gerador virou data-driven; o portão passo [5] agora enforça osm por Na medido em todo exercício.

## 2. Arquitetura-alvo (mescla da §12 dela com o repo atual)

| Alvo dela | Estado no repo |
|---|---|
| `/core cad_core.js policy.json fixtures.json` | ✅ `core/cad_core.js` + `core/fixtures.json`; policy em `canon/policy.json` (mesma função). |
| `/content modules.json cases.json questions.json` | ⬜ hoje o conteúdo vive no HTML e nos `pranchas/*/source.json`. Fase 3 externaliza. |
| `/render generate_pranchas.py build_docx.py build_html.py` | 🟡 `pranchas/*/generate_*.py` existem; `build_html/docx` (injetar POLICY) é Fase 4. |
| `/web index.html app.js styles.css` | 🟡 `app/index.html` single-file (CSS/JS embutidos) — por decisão de offline/iOS. Modularizar é opcional. |
| `/qa test_core.js test_links.js visual_snapshots/` | ✅ `core/cad_core.test.js` + `scripts/test_links.js`; snapshots visuais em CI é Fase 4. |

Divergência consciente do alvo dela: o `app/index.html` **fica single-file** (requisito de offline/iOS/WebKit). Em vez de o HTML *importar* o core, o CANON embutido é **espelho validado pelo portão** — e a Fase 4 fecha isso de vez fazendo o HTML ser *gerado* com a POLICY injetada (CANON como build-artifact, nunca editado à mão).

## 3. Fases (o que está feito × aberto)

- **Fase 0 — Fonte única.** ✅ `canon/policy.json` + `core/` como contrato; `check_consistency.js` barra drift; CI verde.
- **Fase 1 — Hotfix clínico.** ✅ A1, operadores de K (≥), bicarbonato. Render revisado.
- **Fase 2 — Testes.** ✅ 292 asserções do core + 12 fixtures (Δ/Δ 0,71/0,93/1,0/2,1, osm, K) + links.
- **Fase 3 — Dados.** ✅ `content/questions.json` (banco do provão), `content/cases.json` (casos socráticos A/B) e `content/atlas.json` (cálculo, escada de leitura, fenótipos, leis, projetos) extraídos do HTML; o app lê tudo de blocos `<script type="application/json">` **injetados pelo build** a partir das fontes (`build_app.js`), com `--check` no CI e regressão de DOM (idêntico ao pré-refatoração). `content/profiles.json` (8 fenótipos, fisiopatologia+tratamento+caso sintético) segue o mesmo mecanismo em `perfis/index.html`; `scripts/check_profiles.js` recalcula cada caso pelo core e confere as afirmações `hasDka()`/`isResolvedDka()` no texto — anti-drift também na prosa clínica, não só nos limiares. Aberto só: um build que gere o `docs/auditoria.docx` a partir de `content/*.json` (hoje ainda é editado à mão, ver `docs/auditoria-erratum.md` §"Estado dos binários" para o pipeline atual de regeneração do PDF).
- **Fase 4 — UI/render.** 🟡 (a) 🟡 `scripts/build_app.js` regenera o bloco canon a partir de `core.POLICY` (CANON = **artefato de build**, `--check` no CI barra edição à mão); injetar o **conteúdo pedagógico** (casos/questões) continua na Fase 3; (b) ✅ fallback textual + tabela para os 2 canvas (geradas do mesmo modelo, anti-drift); (c) ✅ dois níveis no CI: `scripts/smoke_app.js` (estrutural, browserless) + `scripts/render_smoke.js` (job `render` — executa as saídas num Chromium e falha em erro de JS/console, página em branco ou container vazio). Comparação de **pixel** foi deliberadamente evitada (baseline instável entre ambientes → flakiness); o render-smoke cobre o risco real (quebra de runtime) de forma determinística.
- **Fase 5 — Release.** 🟡 ✅ Changelog clínico (`CHANGELOG.md`, fonte/data/impacto); ✅ pacote ZIP versionado (`npm run package` → `dist/cad360-<versão>.zip` via `git archive`); checklist abaixo mantido.
- **Fase 6 — Tutor de classificação.** ✅ `core.classifyDkaProfile()` — diferencial determinístico (labs → perfis plausíveis + motivo + valores computados), nunca veredito único de caixa-preta; entrada insuficiente aponta o que falta, sem adivinhar. UMD mínimo no core (`window.CadCore` quando carregado via `<script src>`, preservando `module.exports` — sem duplicar fórmulas entre `core/` e HTML). UI em `perfis/index.html`: digite os labs, o Tutor refere ao perfil certo. Regressão: os 8 casos sintéticos se classificam de volta para o próprio perfil (`check_profiles.js`); `cad_core.test.js` cobre entrada insuficiente, multi-match e "sem critério ativo". **Cetonúria (cruzes) como eixo cetônico primário**: `POLICY.diagnosis.ketonuriaCruzesAtLeast = 2` (mirrorado em `canon/policy.json`, com checagem no portão); `hasDka()`/`classifyDkaProfile()` aceitam β-HB **ou** cetonúria — mas `isResolvedDka()` continua exigindo β-HB sérico, deliberadamente (cetonúria mede acetoacetato, que pode subir durante o tratamento enquanto o β-HB cai; não confirma resolução). Sem β-HB, o Tutor mostra a incerteza explicitamente (`isResolvedDka: null`) e, no caso ambíguo parcial×hiperclorêmica, apresenta os dois perfis com a ressalva em vez de escolher um calado.
- **Fase 7 — Tutor no tratado, exceções de manejo e 9º perfil.** ✅ O mesmo Tutor de classificação passa a existir também em `tratado/index.html` (seção "Marco", logo após o índice) — mesmo `core.classifyDkaProfile()`, zero fórmula duplicada; `build_app.js` ganha o bloco `profile-names-data` (`{id,nome}` projetado de `content/profiles.json`) para isso. ✅ Exceções de manejo documentadas no tratado, cada uma rotulada quanto ao seu estatuto epistêmico: (a) **CAD severa + K baixo** — insulina em taxa reduzida já com K 3,0–3,4, em paralelo com reposição agressiva de K, para quadros graves onde não dá para esperar (§5.3; inferência/padrão de prática de UTI, não a doutrina padrão); (b) **dieta oral vs jejum** — critérios de quando manter/impor (nova §5.5); (c) **Ringer lactato vs Ringer simples** — o medo do lactato piorar a acidose é infundado, o medo do K é plausível (porém geralmente pequeno); hipercalemia extrema (sem corte numérico de consenso) é onde o Ringer simples entra, mas ele só resolve o lactato, não o K nem o cloro (§5.1). ✅ **9º perfil: CAD em DRC dialítica** (`content/profiles.json`, `perfis/index.html`) — sem diurese osmótica (glicose sem teto), K sem via de excreção, HCO₃ crônico vs degrau agudo, diálise como via de correção definitiva; `classifyDkaProfile()` ganha o contexto `dialysisDependent` (não um limiar numérico) que desativa a heurística usual de glicose/osm para essa população. Regressão: 9/9 casos em `check_profiles.js`, `cad_core.test.js` cobre o caso de glicose alta não confundir com CAD+HHS quando `dialysisDependent`, `render_smoke.js` cobre o Tutor do tratado e o caso dialítico ponta a ponta.
- **Fase 8 — Auditoria própria pós-Fase 7.** ✅ `docs/auditoria.docx`/`.pdf` reconciliados de fato (a errata anterior alegava correção que não existia — ver CHANGELOG 2026-07-05). ✅ Achado mais sério: o mesmo furo de osm efetiva (Na corrigido dupla-contando a glicose) e o mesmo furo de operador de K (`>` em vez de `≥` no reinício de insulina) estavam **ao vivo no app publicado**, em 4 lugares (`content/atlas.json` banco B-6 + leis[3], `content/questions.json` Q2/Q3, e um exercício hardcoded dentro do próprio `app/index.html` na calculadora de Osmolaridade efetiva — exceção não documentada à regra de fonte única). Todos corrigidos. ✅ Fechado no mesmo dia: `scripts/check_content_text.js` (novo, em `npm run check`/`ci`) varre a prosa livre de `content/*.json` contra as duas classes de erro (operador de K; osm efetiva reusando "Na corrigido" — cruzamento **global** no corpus, já que o furo real era referência cruzada entre exercícios distintos B-5→B-6, não erro dentro de uma única string). ✅ As 5 calculadoras da aba Calcular (não só a de osm efetiva) tinham cada uma seu exercício duplicado a mão, idêntico ao banco B-1..B-6 do Atlas — unificado: `ATLAS` passa a ser parseado antes do array `CALCS`, e uma função `atlasEx(prefixo)` busca o exercício certo do banco por título; `check_content_text.js` barra qualquer `ex:{stem:...}` literal que reapareça, `render_smoke.js` confirma paridade byte-a-byte em runtime.

- **Fase 9 — Gasometria além da CAD (banco de treino, 100/100 completo).** ✅ `core/abg_core.js` (novo): motor stepwise geral de acid-base (Boston/Narins), reusando `anionGap`/`correctedAnionGap`/`round` de `cad_core.js`. Corrigido, antes de escrever qualquer caso, um bug real de branch-order: quando HCO₃/pCO₂ divergem em direções opostas simultaneamente (assinatura de compensação completa, não de disturbio misto), só o pH deve decidir qual é o primário — a ordem fixa dos `if` estava decidindo por inércia, lendo uma alcalose respiratória crônica fisiológica da gestação como "acidose metabólica com compensação excessiva". 16 testes em `abg_core.test.js`. ✅ `content/gasometrias.json` — lotes 1-5 (G-01..G-100): **100 casos sintéticos, 100 diagnósticos diferentes**, cobrindo CAD e seus mimics; toda a família de acidose tubular renal (tipos 1, 2 e 4, por causas idiopáticas, tóxicas e farmacológicas); acidose de AG alto não-cetótica (D-lactato, propilenoglicol, urêmica, CO, cianeto, isopropanol, metformina/MALA, piroglutamicoacidose, malária, tiamina, ferro); acidose hiperclorêmica (fístula pancreática, ureterossigmoidostomia, laxante oculto, nutrição parenteral, VIPoma, ileostomia); alcalose metabólica (realimentação, citrato, bicarbonato exógeno, leite-álcali, pós-hemodiálise, cluster endócrino Bartter/Gitelman/Conn/CAH/alcaçuz, fibrose cística, alcalinização terapêutica no tricíclico); toda a família de falência da "bomba" respiratória (asma, DPOC, SHO, opioide, miastenia, Guillain-Barré, botulismo, ELA, cifoescoliose, baclofeno, benzodiazepínico); os 4 mecanismos de choque com acidose lática (séptico, hipovolêmico, obstrutivo, cardiogênico); o trio hipertermia+rigidez+acidose lática (SNM, hipertermia maligna, síndrome serotoninérgica); distúrbios respiratórios agudos/crônicos (gestação, altitude, hipotermia, cirrose, TCE, tireotoxicose, mixedema — espelho da tireotoxicose, hipertensão intracraniana hepática); os 4 tipos clássicos de hipóxia tecidual (hipoxêmica/CO, anêmica, histotóxica/cianeto, metemoglobinemia); 3 casos de erro pré-analítico puro (venosa rotulada como arterial, bolha de ar, leukocyte larceny); 2 casos de AG mascarado por processo duplo; e um controle negativo genuíno (G-100, gasometria normal, caso de encerramento). Detalhe completo de cada lote em CHANGELOG.md. Nenhum pH hand-typed independente de HCO₃/pCO₂: derivado por Henderson-Hasselbalch. ✅ `scripts/check_gasometrias.js` (em `npm run check`) recalcula todos os 100 casos e cruza contra o gabarito — schema estável desde o lote 1, com um único ajuste em 5 lotes (regex do id para aceitar `G-100`, 3 dígitos). ✅ UI em `painel/index.html`, claramente separada da série real de-identificada — filtro por categoria, reveal socrático com cálculo ao vivo, navegação por `confundeCom`, contador/checagem de render lidos dinamicamente de `GASO.length`. **Fechado**: banco completo em 100/100 casos.

## 4. Checklist de release

- [ ] `npm run ci` verde (core + fixtures + consistência + links).
- [ ] Busca textual não acha `0,4-0,8 pura`, `ureia na efetiva`, `K < 5,5`, `K > 3,3` como instrução.
- [ ] Fronteiras (K, Δ/Δ, HHS, bicarbonato) com operadores padronizados (K = `≥`).
- [ ] Pranchas renderizadas e revisadas; DOCX sem clipping; EX04 regenerado.
- [ ] Changelog atualizado (fonte, data, impacto, artefatos afetados).
- [ ] Regulatório: mantido uso pessoal autoral humano-no-loop; qualquer compartilhamento dispara triagem SaMD (tripwire).
