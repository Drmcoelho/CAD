# CHANGELOG — clínico e de doutrina

Registro versionado de **mudanças de limiar clínico** (o que mudou · de → para · fonte · impacto) e das correções de consistência. Segue a diretriz-mãe: número clínico nasce em `canon/policy.json` + `core/`; tudo abaixo é rastreável até essa fonte.

Fonte de verdade clínica em todas as entradas: **Umpierrez et al. *Diabetes Care* 2024;47(8):1257–1275** (consenso ADA/EASD/JBDS/AACE/DTS), salvo indicação.

---

## [CAD360-ApA-2026-07-01] — doutrina congelada

Versão-base do `POLICY` (`core/cad_core.js`) e espelho `canon/policy.json`. Limiares canônicos, com o valor **obsoleto** que substituem quando aplicável:

| Eixo | De (obsoleto) | Para (canônico) | Fonte · impacto |
|---|---|---|---|
| Potássio — repor | `< 5,5` (JBDS) | **`< 5,0`** | Consenso 2024. Alvo 4–5; repõe mais cedo. |
| Potássio — reiniciar insulina | `≥ 3,3` | **`≥ 3,5`** | Elimina a zona cinzenta; adiar insulina se `< 3,5`. |
| Δ/Δ — faixa "pura" | `0,4–0,8` | **fronteiras 1 e 2** | `< 1` já tem cauda hiperclorêmica; `1–2` limpa; `> 2` alcalose somada. |
| Osm efetiva | `2·Na corrigido + glic/18` | **`2·Na MEDIDO + glic/18`** | Na corrigido dupla-conta a glicose; efetiva = tonicidade. Ureia fora. |
| Bicarbonato | — | **pH `< 7,0`** (consenso) **+ fronteira de evidência `≤ 6,9`** | Não é rotina; abaixo de 6,9 é decisão de UTI, sem RCT. |
| Na corrigido | — | fator **1,6** (**2,4** se glicose `> 400`) | Déficit de água livre — nunca na fórmula de osm. |
| Diagnóstico | — | glicose `≥ 200` (ou DM) · βHB `≥ 3,0` (ou cetonúria `≥ 2+`) · pH `< 7,30` **e/ou** HCO₃ `< 18` | AG fora dos critérios formais. |
| Resolução | glicemia normal | **βHB `< 0,6` E (pH `≥ 7,3` OU HCO₃ `≥ 18`)** | Glicemia normal não é resolução. |
| HHS | — | osm efetiva `> 320` (comentário 2024 propõe `> 300`) | 320 é critério; 300 é alerta. |
| Insulina | — | 0,1 → 0,05 U/kg/h quando glicose `< 250` + dextrose | Dextrose = cinto de segurança. |

Cobertura: `core/cad_core.test.js` (asserções do core) + `core/fixtures.json` (Δ/Δ 0,71 / 0,93 / 1,0 / 2,1 · osm · K), recomputadas pelo core. Portão `scripts/check_consistency.js` barra qualquer divergência entre core, `policy.json`, pranchas e `app/`.

## [2026-07-01] — hotfix clínico (Fase 1, auditoria externa)

- **Δ/Δ 0,93** rotulado "quase pura" → **"limítrofe (`< 1`), já com cauda hiperclorêmica"**. Não chamar de pura sem olhar Cl⁻/tendência.
- **Operadores de K** padronizados para `≥` (reinício `K ≥ 3,5`; banda inicial `K ≥ 5,0`), consistentes com o core.
- **Bicarbonato**: frase seca substituída por "decisão de terapia intensiva conforme gravidade/protocolo local/risco".
- **EX04 (CAD+HHS)**: osm efetiva corrigida de `2·142+600/18 = 317,3` (Na corrigido) para `2·130+600/18 = 293,3` (Na medido). Gerador virou data-driven; portão passo [5] enforça osm por Na medido em todo exercício.

## [2026-07-02] — importação, infraestrutura e revisão

- **Projeto importado para a raiz** do repositório (serve via GitHub Pages).
- **Infra**: deploy de Pages + `.nojekyll`; `LICENSE` CC BY-NC-SA 4.0; template de PR; badges; `engines.node`.
- **Portão [7]**: `app/index.html` expõe o `POLICY` como bloco `<script type="application/json" id="canon">`, validado por deep-equal contra o core (valor-a-valor, não mais só presença de token).
- **`docs/auditoria-erratum.md`**: reconcilia o corpo da auditoria com o canon (Δ/Δ 0,93 = limítrofe; bicarbonato ≤ 6,9; compensação dos 4 distúrbios).
- **Compensação (aba Fisiologia)**: no eixo HCO₃, medido acima do esperado = **alcalose** metabólica associada; abaixo = **acidose** metabólica associada (rótulo e cor estavam invertidos).
- **SVG lote1**: `marker-end` sem fecho de `url()` — setas não renderizavam; corrigido e SVGs regenerados.
- **`hasDka` / `isResolvedDka`**: eixo ácido agora opcional (basta pH **ou** HCO₃, refletindo o "e/ou" do critério), com guarda que lança erro claro se faltarem ambos.

## [2026-07-03..04] — publicação

- **GitHub Pages no ar**: `configure-pages` com `enablement: true` (deploy à prova de timing); site em https://drmcoelho.github.io/CAD/. Deploy automático a cada push em `main`.

## [2026-07-04] — Fase 3 completa, perfis de CAD, deep-link

- **Conteúdo pedagógico externalizado por completo**: `content/questions.json` (provão), `content/cases.json` (casos socráticos), `content/atlas.json` (banco de cálculo, escada, fenótipos, leis, projetos) — todos lidos pelo `app/` via blocos `<script type="application/json">` injetados por `build_app.js` (`--check` no CI barra edição à mão).
- **`content/profiles.json` + `perfis/index.html`**: fisiopatologia detalhada e tratamento por eixo dos **8 fenótipos de CAD** (clássica, euglicêmica, pré-CAD, parcialmente tratada, CAD+HHS, hiperclorêmica tardia, +sepse/lactato, alcoólica/jejum), cada um com marcadores, armadilha nomeada e incerteza etiquetada.
- **Caso sintético por perfil**: cada perfil ganha um caso com labs estruturados, calculado pelo mesmo `core/cad_core.js` (não transcrição). `scripts/check_profiles.js` recalcula AG/AGc/`hasDka()`/`isResolvedDka()` a partir dos labs e confere contra o texto — quebra o build se um número for editado sem recalcular o outro.
- **Deep-link app ↔ perfis**: cada caso tem um botão que abre a calculadora do `app/` já preenchida com os números do caso (`#calcular?na=..&cl=..&hco3=..&alb=..&glu=..`), verificado ponta a ponta (headless).
- **Cross-link com o `tratado/`**: §1.6 (Variantes) e §5 (Manejo) agora apontam para `perfis/`, e `perfis/` aponta de volta para o tratado.
- **CI**: `render_smoke.js` (Fase 4c) executa as saídas num Chromium e falha em erro de runtime; `check_profiles.js` no `npm run check`.

## [2026-07-04] — Tutor de classificação de perfil

- **`core/cad_core.js` ganha `classifyDkaProfile()`**: diferencial determinístico de perfil de CAD a partir de labs — devolve uma lista **ranqueada** de perfis plausíveis com o motivo de cada um (nunca uma caixa-preta de veredito único), mais os valores computados (AG, AGc, Δ/Δ, osm efetiva, `hasDka()`, `isResolvedDka()`). Entrada insuficiente devolve os campos que faltam, em vez de adivinhar.
- **UMD mínimo no core**: `core/cad_core.js` passa a expor `window.CadCore` quando carregado via `<script src>` em página estática (sem bundler), preservando `module.exports` para Node/CI — mesmas fórmulas, zero duplicação entre `core/` e o HTML.
- **Tutor em `perfis/index.html`**: formulário no topo da página — digite os labs, o classificador aponta o(s) perfil(is) mais prováveis com o porquê e um link direto para a seção certa.
- **Regressão do classificador**: `scripts/check_profiles.js` confirma que os 8 casos sintéticos já existentes se classificam de volta para o **próprio** perfil (top match); `core/cad_core.test.js` cobre entrada insuficiente, múltiplos matches e o caso "sem critério de CAD ativo" (matches vazio, sem forçar rótulo).
- **CI**: `render_smoke.js` preenche o Tutor com o caso clássico e confirma a classificação ponta a ponta (com `window.CadCore` real, no navegador).

## [2026-07-04] — cetonúria (cruzes) como eixo cetônico primário

Correção de realidade clínica: β-HB sérico point-of-care **não é padrão no Brasil**; o que existe na prática é a fita de cetonúria em cruzes (1+ a 4+). O código tratava β-HB como obrigatório — a doutrina já registrava a alternativa ("βHB ≥3,0 **ou** cetonúria ≥2+"), mas não estava implementada.

| Eixo | De | Para | Fonte · impacto |
|---|---|---|---|
| Diagnóstico (eixo cetônico) | β-HB obrigatório | **β-HB OU cetonúria ≥2+** (`POLICY.diagnosis.ketonuriaCruzesAtLeast = 2`) | `hasDka()` e `classifyDkaProfile()` aceitam qualquer um dos dois; cetonúria vira o campo primário do Tutor. |
| Resolução | β-HB obrigatório | **inalterado, deliberadamente** — cetonúria não confirma resolução | Cetonúria mede acetoacetato; durante o tratamento o β-HB se converte em acetoacetato, então a cetonúria pode **piorar** enquanto o paciente melhora. `isResolvedDka()` continua exigindo β-HB sérico; sem ele, `classifyDkaProfile()` devolve `isResolvedDka: null` (incerto) e — no caso ambíguo entre "parcialmente tratada" e "hiperclorêmica" — mostra **ambos** os perfis com a ressalva, em vez de escolher um calado. |

- **Achado extra corrigido**: `canon/policy.json` já tinha um campo `"ketonuriaAtLeast": "2+"` (string) sem equivalente no `core/cad_core.js` — divergência latente que o portão não pegava. Agora `ketonuriaCruzesAtLeast: 2` (número) existe nos dois lados, com checagem de igualdade em `check_consistency.js`.
- **Tutor**: cetonúria (cruzes) é o campo primário; β-HB sérico vira secundário/opcional, com nota explícita de que substitui a cetonúria quando disponível, e um aviso amarelo nomeando a armadilha do acetoacetato.
- Testado ponta a ponta (navegador real): classificação correta usando **só** cetonúria, sem β-HB, e o painel computado mostra qual eixo foi usado + "incerto (sem β-HB sérico)" quando aplicável.

## [2026-07-04] — Tutor no tratado, exceções de manejo e 9º perfil (CAD + DRC dialítica)

- **Tutor no `tratado/index.html`**: nova seção "Marco — classifique o perfil antes de ler", logo após o índice, com o mesmo `classifyDkaProfile()` (`core/cad_core.js` carregado via `<script src>`, zero fórmula duplicada). `scripts/build_app.js` ganha um bloco `profile-names-data` (`{id, nome}`, projetado de `content/profiles.json` — a prosa completa não é duplicada) para o Tutor do tratado rotular o match e linkar para `../perfis/#p-<id>`. Testado ponta a ponta (`render_smoke.js`) e estruturalmente (`smoke_app.js`).
- **Exceção de manejo — CAD severa com K baixo**: novo box em §5.3 (Potássio) documentando o padrão de prática de UTI para quadros verdadeiramente graves onde não dá para atrasar a insulina à espera do K subir — insulina em taxa reduzida já com K 3,0–3,4, **em paralelo** (não em sequência) com reposição agressiva de K. Rotulado explicitamente como **inferência/padrão de prática de terapia intensiva**, não a doutrina padrão desta ferramenta (que segue segurar insulina `<3,5` para o caso geral).
- **Dieta oral vs jejum** (nova §5.5 do tratado): critérios de quando manter via oral (CAD leve/moderada, alerta, estável) vs impor jejum (rebaixamento, vômitos persistentes, íleo, HHS com sensório comprometido). Bicarbonato renumerado de §5.5 para §5.6 (sem mudança de conteúdo).
- **Ringer lactato vs Ringer simples** (extensão de §5.1): tabela de composição ganha a linha "Ringer simples (s/ tampão)" (Na 147 · Cl 156 · sem lactato). Novo box distingue dois vieses contra o Ringer lactato — o medo do lactato "piorar a acidose" é **infundado** (o lactato exógeno vira bicarbonato hepático); o medo do K (~4 mEq/L) num paciente hipercalêmico é **plausível**, embora geralmente pequeno. Hipercalemia extrema (sem corte numérico único de consenso — descrito qualitativamente, ex. 6,0–6,5) é a situação em que o Ringer simples entra como alternativa — mas ele resolve o desconforto com o lactato, não o K nem o cloro (carga comparável ao SF). Tudo rotulado como inferência/padrão de prática, fora do `core`.
- **9º perfil: CAD em DRC dialítica (hemodiálise)** (`content/profiles.json`, `perfis/index.html`): fisiopatologia própria (sem diurese osmótica → glicose sem teto; K sem via de excreção; HCO₃ crônico vs degrau agudo; diálise como via de correção definitiva), tratamento por eixo, armadilha, incerteza e caso sintético (glicose 780, K 6,3, pH 6,98 — recalculado pelo core: AG 36 · AGc 38 · Δ/Δ 1,63 · osm efetiva 319,3). Cruza com as seções novas do tratado (§5.1 Ringer/hipercalemia, §5.3 exceção K/insulina).
- **`classifyDkaProfile()` ganha `dialysisDependent`** (contexto, não limiar numérico): quando verdadeiro, aponta direto para `"dialitica"` em vez de aplicar a heurística usual de glicose/osm para euglicêmica/CAD+HHS — que não vale nessa população (sem clearance renal de glicose). `sepse-lactato` continua compondo o diferencial normalmente (eixo ortogonal ao contexto renal). Regressão: `core/cad_core.test.js` (glicose 780 não força `cad-hhs`) e `scripts/check_profiles.js` (9º caso classifica de volta para `dialitica`).
- **CI**: `render_smoke.js` cobre o Tutor do tratado e o caso de DRC dialítica (glicose alta não confunde com CAD+HHS); `smoke_app.js` e `check_profiles.js` atualizados de 8 para 9 perfis/casos.

## [2026-07-05] — `docs/auditoria.docx`/`.pdf` de fato reconciliados (a errata anterior estava incompleta)

Auditoria própria pós-PR#15: a alegação em `docs/auditoria-erratum.md` de que os pontos 1–2 (Δ/Δ 0,93, bicarbonato 6,9) "já estavam aplicados no `.docx`-fonte" era **falsa** — extração direta do XML do `.docx` mostrou "quase pura" e nenhuma menção a "6,9". Corrigido de fato, mais 2 achados que a errata original não cobria:

| Achado | De | Para | Impacto |
|---|---|---|---|
| Δ/Δ 0,93 (worked case) | "AGMA quase pura" | "AGMA **limítrofe** (Δ/Δ < 1: já há componente hiperclorêmico...)" | Era alegado como corrigido; não estava. |
| Bicarbonato (2 menções) | "somente pH < 7,0" | + "sem benefício demonstrado acima de 6,9 — abaixo disso, decisão de UTI" | Idem — alegado, não aplicado. |
| Exercício B-6 (osm efetiva) | `2·142 (Na corrigido) + 600/18 = 317` | `2·130 (Na MEDIDO) + 600/18 = 293,3` | **Mesmo furo do EX04** (ROADMAP §1), sobrevivendo intacto neste exercício — dupla-contava a glicose. |
| Apêndice A (potássio) | "ADIAR insulina até **> 3,5**" | "ADIAR insulina até **≥ 3,5**" | Reabria a zona cinzenta em K=3,5 que a Fase 1 eliminou em todo o resto do repo. |

- **`docs/auditoria.pdf` regenerado** a partir do `.docx` corrigido. `soffice`/LibreOffice está instalado no ambiente desta sessão, mas sua conversão headless está **quebrada** (`soffice --headless --convert-to pdf` falha até para um `.txt` trivial — confirmado não ser problema do arquivo). Fallback usado: `pandoc` (`.docx`→HTML) + Chromium headless (HTML→PDF via `page.pdf()`), com CSS mínimo para paginação A4/tabelas/tipografia. Conteúdo conferido (extração de texto do PDF batendo com as 6 correções); diagramação **não é idêntica** à exportação nativa do Word/LibreOffice — documentado como provisório em `docs/auditoria-erratum.md`.
- `docs/auditoria-erratum.md` atualizado: pontos 1–2 marcados `✅ aplicado`, pontos 4–5 (achados novos) adicionados, rodapé reescrito para descrever o pipeline real usado.

## [2026-07-05] — mesmo furo de osm efetiva e o mesmo furo de operador de K, achados no app AO VIVO

Continuação da auditoria própria: os dois furos corrigidos em `docs/auditoria.docx` (osm efetiva dupla-contando a glicose; operador `>` em vez de `≥` no reinício de insulina) **também estavam no app publicado** (`drmcoelho.github.io/CAD/`), em quatro lugares — mais grave que o `.docx` (documento estático), porque é a ferramenta em uso. Todos corrigidos na fonte e regenerados via `build_app.js`:

| Local | De | Para |
|---|---|---|
| `content/atlas.json` — banco B-6 (aba Atlas) | premissa "Na 142..." + gabarito `2·142+600/18=317` | premissa esclarece Na medido 130 (corrigido 142 é do exercício anterior) + gabarito `2·130+600/18=293,3` |
| `content/atlas.json` — leis[3] (aba Atlas, "12 leis") | "reiniciar > 3,5" | "reiniciar ≥ 3,5" |
| `content/questions.json` — Q2 exp (Provão) | "reiniciar a insulina acima de 3,5" (a MCQ já tinha a opção correta "≥3,5" — a explicação contradizia a própria resposta certa) | "reiniciar a insulina ao atingir K ≥ 3,5" |
| `content/questions.json` — Q3 exp (Provão, item V/F) | "o canon 2024 usa > 3,5 como o número único de reinício" | "usa ≥ 3,5" |
| `app/index.html` — calculadora "Osmolaridade efetiva" (aba Calcular), exercício inline | hardcoded no próprio `app/index.html` (fora de `content/*.json`, exceção não documentada à regra de fonte única): `2×142+600/18=317` | `2×130 (Na MEDIDO)+600/18=293,3`, com nota de que o Na corrigido dobraria a contagem |

- **Achado estrutural**: a calculadora "Osmolaridade efetiva" da aba Calcular carrega seu exercício-exemplo como string literal dentro do `<script>` do `app/index.html` — não vem de `content/atlas.json`. É uma exceção não documentada à regra-mãe ("nenhum número clínico nasce hardcoded em HTML"); o conteúdo foi corrigido no lugar, mas a duplicação estrutural entre o exemplo desta calculadora e o banco B-6 do Atlas (mesmo conceito, duas fontes) permanece — candidato a unificação numa Fase futura.
- Nenhum destes 4 pontos era coberto por `check_consistency.js`/`check_profiles.js` (que verificam `POLICY` deep-equal e os 9 casos sintéticos, não a prosa livre de `atlas.banco`/`atlas.leis`/`questions`) — por isso sobreviveram sem detecção automática. Ver ROADMAP para um possível portão futuro de regex sobre esse conteúdo.
- Verificado: `npm run ci` verde, `build_app.js --check` sincronizado, `render_smoke.js` verde, e render manual confirmando os textos corrigidos nas abas Atlas/Provão/Calcular do app real.

## [2026-07-05] — portão anti-recorrência + fim da duplicação de exercícios

Fecha as duas aberturas deixadas pela auditoria de 2026-07-05 (osm efetiva/operador de K).

- **`scripts/check_content_text.js`** (novo, entra em `npm run check`/`ci`): varre toda a prosa livre de `content/*.json` (que `check_consistency.js`/`check_profiles.js` não cobrem — aqueles validam `POLICY` deep-equal e os 9 casos estruturados, não `atlas.banco`/`atlas.leis`/`questions.exp`) contra as duas classes de erro já vistas: (1) operador `>` em vez de `≥` no reinício de insulina; (2) a fórmula de osm efetiva reusando um valor que apareceu em QUALQUER outro trecho do corpus como resultado de "Na corrigido" — cruzamento **global**, porque o furo real de 2026-07-05 era uma referência cruzada entre dois exercícios distintos (B-5 → B-6), não um erro dentro de uma única string. Testado com os dois furos reais reintroduzidos artificialmente (confirma detecção) e com o estado atual (confirma zero falso positivo).
- **Fim da duplicação das calculadoras**: as 5 calculadoras da aba Calcular (AG, AGc, Δ/Δ, Na corrigido, Osmolaridade efetiva) tinham cada uma seu próprio exercício-exemplo **hardcoded** em `app/index.html`, idêntico em número ao banco B-1..B-6 do Atlas (`content/atlas.json`) — duplicação total, não só a da osm efetiva. `ATLAS` (antes parseado só perto do fim do script) passa a ser parseado antes do array `CALCS`; uma função `atlasEx(prefixo)` busca o exercício certo do banco pelo título — as 5 calculadoras agora leem `ex:atlasEx('B-1')` etc. em vez de repetir o texto. `check_content_text.js` também barra qualquer `ex:{stem:...}` literal que reapareça em `CALCS`; `render_smoke.js` confirma em runtime que o exercício da calculadora de osm efetiva é byte-a-byte igual ao banco B-6.
- Verificado: `npm run ci` verde, `render_smoke.js` verde (incluindo o novo check de paridade Calcular×Atlas), render manual das 5 calculadoras + banco confirmando texto idêntico lado a lado.

## [2026-07-05] — `core/abg_core.js` (classificador geral de gasometria) + banco de gasometrias, lote 1 (20 casos)

Início da exploração de gasometria/bioquímica além da CAD, incluindo distúrbios que não são CAD e alguns desenhados para confundir com ela.

- **`core/abg_core.js`** (novo): motor de acid-base GERAL (não específico de CAD) — dado pH/pCO₂/HCO₃, resolve o(s) distúrbio(s) primário(s), checa a compensação esperada (Winter para acidose metabólica e fórmulas análogas para alcalose metabólica/acidose e alcalose respiratória aguda×crônica) e sinaliza um segundo processo escondido atrás de um pH que parece normal. Reusa `anionGap`/`correctedAnionGap`/`round` de `core/cad_core.js` (uma fonte, nunca duplicada). HCO₃ e pCO₂ anormais na MESMA direção nunca é compensação — é sempre misto; sem `chronicityHint` num distúrbio respiratório isolado, mostra as duas janelas (aguda/crônica) em vez de adivinhar (mesma filosofia de `classifyDkaProfile()`: nunca um veredito único de caixa-preta).
  - **Bug real corrigido antes de escrever qualquer caso**: quando HCO₃ e pCO₂ são anormais em direções OPOSTAS simultaneamente (ex.: HCO₃ baixo + pCO₂ baixo — a assinatura de uma compensação completa, em qualquer sentido), a ordem fixa dos branches decidia o primário por inércia da ordem do código, não pela direção do pH — uma alcalose respiratória crônica fisiológica da gestação (pH 7,46, pCO₂ 30, HCO₃ 20) era lida como "acidose metabólica com compensação excessiva", o inverso da leitura clínica correta. Corrigido: agora o pH decide qual lado é primário quando os dois eixos divergem em direções opostas; com pH normal (compensação completa nos dois sentidos), o motor mostra as DUAS leituras possíveis, honesto sobre a ambiguidade em vez de escolher uma calado. 16 testes em `core/abg_core.test.js`, incluindo o par de regressão desse bug e seu simétrico.
- **`painel/index.html`** carrega `cad_core.js` + `abg_core.js` via `<script src>` e usa `AbgCore.expectedPco2MetabolicAcidosis()` em vez de reimplementar Winter inline (eliminação de mais uma duplicação de fórmula).
- **`content/gasometrias.json`** (novo, lote 1 de 20 casos rumo a 100+): casos sintéticos e de-identificados, 20 diagnósticos diferentes — CAD e seus mimics (euglicêmica por iSGLT2, HHS, cetoacidose alcoólica, cetose de jejum), acidose de AG alto não-cetótica (sepse/lactato, salicilato, metanol/etilenoglicol), acidose hiperclorêmica (diarreia, RTA1, RTA4), alcalose metabólica (vômitos, diuréticos), distúrbios respiratórios agudos/crônicos (asma em duas fases da mesma doença, DPOC, gestação, TEP, ansiedade) e um caso de AG mascarado por processo duplo (alcalose de vômitos escondendo CAD incipiente). Pares deliberadamente confundíveis (G-17/18/19, G-12/13, G-09/10/11) só se resolvem por um detalhe propedêutico, nunca pelo número isolado. Nenhum número nasce solto: cada trinca pH/pCO₂/HCO₃ é derivada por Henderson-Hasselbalch a partir de HCO₃/pCO₂ escolhidos (nunca os três digitados independentemente), e cada afirmação do gabarito é recomputada pelos mesmos `abg_core.js`/`cad_core.js`.
- **`scripts/check_gasometrias.js`** (novo, em `npm run check`/`ci`): recalcula os 20 casos e cruza contra o gabarito — consistência de Henderson-Hasselbalch, distúrbio(s) primário(s) calculado(s) batendo com o texto, AG/AGc rastreáveis no gabarito, e `hasDkaEsperado` (campo estrutural) batendo com `cad_core.hasDka()`. Mesma filosofia de `check_profiles.js`.
- **UI em `painel/index.html`**: nova seção "Banco de gasometrias — treino (sintético)", claramente separada (título + aviso + `<hr>`) da série real de-identificada já existente na página. Filtro por categoria, grade de 20 casos com badge "imita CAD", detalhe do caso com vinheta + labs brutos + pergunta, e reveal socrático (o aluno decide antes de ver a resposta); ao revelar, recalcula ao vivo via `AbgCore`/`CadCore` antes de mostrar a prosa do gabarito, e lista os `confundeCom` como navegação direta entre os pares. `render_smoke.js` ganha uma seção "painel" (antes ausente do smoke) cobrindo os 20 casos, o reveal, a navegação por confundeCom e o filtro por categoria.
- Verificado: `npm run ci` verde (292+12+16 asserções de core/fixtures/abg_core, `check_gasometrias.js` com os 20 casos), `render_smoke.js` verde, screenshots desktop e iPhone confirmando o layout e a separação visual da série real.

## [2026-07-05] — banco de gasometrias, lote 2 (+20 casos, total 40/100+)

- **`content/gasometrias.json`** ganha 20 novos casos (G-21..G-40), 20 diagnósticos adicionais, nenhum repetido dos 20 do lote 1: acidose urêmica de DRC avançada (AG alto — contraste com as RTA de AG normal do lote 1), acidose D-lática (ponto cego enantiomérico do ensaio padrão de lactato), propilenoglicol (excipiente de infusão prolongada de benzodiazepínico), rabdomiólise com LRA, cetose de jejum extremo com hipoalbuminemia mascarando o AG bruto (anorexia nervosa grave), síndrome de realimentação, alcalose por citrato pós-transfusão maciça, alcalose pós-hipercápnica iatrogênica ("overshoot" após correção ventilatória rápida de DPOC — par direto do G-15 do lote 1), SHO/AOS (gêmeo numérico do DPOC, resolvido só por história clínica), depressão respiratória por opioide, alcalose respiratória crônica da grande altitude (mesmo tipo de distúrbio do G-16, gatilho hipóxico em vez de hormonal), CAD euglicêmica por jejum pré-operatório sem iSGLT2 (mecanismo distinto do G-02), RTA tóxica por toluenismo, acidose hiperclorêmica por fístula pancreática externa, alcalose por ingestão exógena de bicarbonato (AG normal, ao contrário da discreta elevação por contração de volume dos casos G-12/13), crise addisoniana (mimetiza RTA4 mas com hiponatremia/hipotensão que a RTA4 isolada não explica), gasometria na hipotermia acidental (caso sobre COMO MEDIR/interpretar — alpha-stat vs pH-stat — não sobre qual é o distúrbio primário), acidose mista pós-parada cardiorrespiratória (mesma direção, nunca compensação — contraste direto com a acidose lática pura do G-06), hiperventilação por dor pós-operatória não reconhecida (5º gatilho diferente do mesmo padrão gasométrico dos casos G-17/18/19/31) e cetoacidose por diabetes esteroide (contraste espelhado com o G-04: aqui o eixo glicêmico fecha pelo VALOR absoluto de glicose, não por diagnóstico prévio de diabetes — o oposto do que falha em G-04).
- Todos os pH derivados por Henderson-Hasselbalch a partir de HCO₃/pCO₂ (nunca os três digitados independentemente); `scripts/check_gasometrias.js` recalcula e cruza os 40 casos.
- Corrigidos dois números hardcoded que ficariam obsoletos a cada lote novo: o contador "N casos" na introdução de `painel/index.html` agora lê `GASO.length` em vez de "20" fixo; `scripts/render_smoke.js` compara a contagem de cards renderizados contra `GASO.length` em vez de um `=== 20` fixo.
- Verificado: `npm run ci` verde (`check_gasometrias.js` com os 40 casos), `render_smoke.js` verde, render manual confirmando os 40 cards e as 8 categorias agregando corretamente.

## [2026-07-05] — banco de gasometrias, lote 3 (+20 casos, total 60/100+)

- **`content/gasometrias.json`** ganha 20 novos casos (G-41..G-60), todos NÃO-CAD — aprofundando o lado do banco mais explicitamente pedido pelo usuário ("gasometrias que não são CAD"): dois pares de "medição engana" (monóxido de carbono/cianeto com oxigenação medida normal apesar de hipóxia real, ecoando a hipotermia do lote 2), isopropanol (cetose SEM acidose — o único álcool tóxico clássico cujo metabólito final não é ácido, contraste direto com o metanol/etilenoglicol do lote 1), cirrose hepática e traumatismo cranioencefálico como 3ª e 4ª causas de alcalose respiratória crônica/aguda (mecanismo hepático/estrutural, somando-se à gestação e à altitude dos lotes anteriores), um cluster de 5 casos de alcalose metabólica hipocalêmica numericamente quase idênticos resolvidos só por propedêutica/endocrinologia (síndrome de Bartter, síndrome de Gitelman, hiperaldosteronismo primário/Conn, hiperplasia adrenal congênita por deficiência de 11-beta-hidroxilase — espelho mecânico direto da crise addisoniana do lote 2 — e pseudo-hiperaldosteronismo por alcaçuz/glicirrizina), penicilina em alta dose como causa farmacológica "Bartter-like" adquirida, estenose hipertrófica do piloro e intoxicação salicílica pediátrica (contraste de fenótipo clínico com os correspondentes casos adultos dos lotes 1-2), ureterossigmoidostomia e abuso oculto de laxantes (2ª e 3ª causas de acidose hiperclorêmica além da diarreia simples), choque de queimadura e alcalose pós-circulação-extracorpórea (2 novos contextos cirúrgicos), alcalinização terapêutica DELIBERADA com bicarbonato na intoxicação por antidepressivo tricíclico (a alcalose É o tratamento, não o problema a corrigir), crise miastênica (4º mecanismo distinto de falência da "bomba" respiratória, ao lado de asma/SHO/opioide dos lotes anteriores) e intoxicação por teofilina (2º tóxico com o mesmo padrão numericamente ambíguo do salicilato, por mecanismo farmacológico diferente).
- Mesma disciplina dos lotes 1-2: todo pH derivado por Henderson-Hasselbalch a partir de HCO₃/pCO₂; `scripts/check_gasometrias.js` recalcula e cruza os 60 casos sem nenhuma mudança no script (schema estável desde o lote 1).
- Verificado: `npm run ci` verde (`check_gasometrias.js` com os 60 casos), `render_smoke.js` verde, render manual confirmando os 60 cards e as 8 categorias agregando corretamente.

## [2026-07-06] — banco de gasometrias, lote 4 (+20 casos, total 80/100+)

- **`content/gasometrias.json`** ganha 20 novos casos (G-61..G-80): completa o trio de acidose tubular renal (tipo 2/proximal — idiopática/Fanconi, induzida por ifosfamida, induzida por acetazolamida — a mesma falha da anidrase carbônica proximal por três vias: genética, tóxica-quimioterápica, farmacológica), fibrose cística como 3ª via de perda de cloreto ("pseudo-Bartter" pela pele, não pelo rim/GI dos casos já existentes), seis causas de acidose lática não-cetótica novas (metformina/MALA, piroglutamicoacidose por paracetamol crônico, malária grave, deficiência de tiamina não-alcoólica, intoxicação por ferro, somando-se às já existentes), acidose hiperclorêmica iatrogênica por nutrição parenteral total, o trio de falência neuromuscular respiratória aguda (Guillain-Barré, botulismo — três níveis anatômicos diferentes da mesma "bomba quebrada" que a miastenia já existente) mais esclerose lateral amiotrófica (versão crônica) e cifoescoliose grave (4º mecanismo, puramente mecânico-restritivo), um segundo e terceiro caso de "a oximetria engana" (metemoglobinemia — platô falsamente baixo, o oposto do monóxido de carbono do lote 3 — e hipóxia anêmica por hemorragia maciça, onde a SpO₂ está genuinamente correta mas o conteúdo de oxigênio é baixo, completando os 4 tipos clássicos de hipóxia tecidual), três casos inteiramente novos sobre erro PRÉ-ANALÍTICO (amostra venosa rotulada como arterial, bolha de ar contaminando a seringa, "leukocyte larceny" por leucocitose extrema consumindo oxigênio in vitro) — nos três, não há distúrbio a diagnosticar no paciente, o achado é reconhecer que a amostra está errada — e tempestade tireoidiana como 7º gatilho diferente do mesmo padrão numérico de alcalose respiratória aguda já usado repetidamente no banco.
- Mesma disciplina dos lotes anteriores: todo pH derivado por Henderson-Hasselbalch; `scripts/check_gasometrias.js` recalcula e cruza os 80 casos sem nenhuma mudança no script (schema estável desde o lote 1, 4 lotes consecutivos).
- Verificado: `npm run ci` verde (`check_gasometrias.js` com os 80 casos), `render_smoke.js` verde, render manual confirmando os 80 cards e as 8 categorias agregando corretamente.

## [2026-07-06] — banco de gasometrias, lote 5 (+20 casos) — COMPLETA 100/100

- **`content/gasometrias.json`** ganha os 20 casos finais (G-81..G-100), fechando o banco combinado com o usuário desde a proposta inicial ("comece com 20 depois seguimos com os próximos até a centena"): síndrome leite-álcali e alcalose pós-hemodiálise; CAD por clozapina e CAD precipitada por pancreatite aguda (2 novos gatilhos de CAD verdadeira sem diabetes prévio); o trio hipertermia+rigidez+acidose lática (síndrome neuroléptica maligna, hipertermia maligna — a única com acidose MISTA, mais fulminante — e síndrome serotoninérgica, cada uma com agente/ritmo de instalação/achado de exame diferente); acidose lática pós-crise convulsiva (contraexemplo deliberado de quando NÃO investigar agressivamente) e TEP maciço com choque obstrutivo (progressão/descompensação do TEP do lote 1); VIPoma e ileostomia de alto débito (4ª/5ª causas de acidose hiperclorêmica intestinal); doença pulmonar intersticial inicial e síndrome de hiperventilação crônica (mais variantes do padrão de alcalose respiratória); choque cardiogênico (4º mecanismo de choque com acidose lática, junto a distributivo/hipovolêmico/obstrutivo já cobertos); coma mixedematoso (espelho mecânico direto da tempestade tireoidiana do lote 3); baclofeno e benzodiazepínico isolado (2 novos depressores centrais, mecanismos/margens de segurança diferentes do opioide); hipertensão intracraniana por insuficiência hepática fulminante (2ª causa de hiperventilação neurogênica central, difusa/metabólica em vez de focal/traumática como o TCE); afogamento com aspiração (3º caso de acidose mista na mesma direção, nunca compensação); e G-100, caso de encerramento — uma gasometria genuinamente NORMAL, controle negativo deliberado, contrastando explicitamente com G-41/G-77 (mesmo achado superficial "tudo normal", três motivos completamente diferentes) e fechando o banco com uma nota resumindo o método usado nos 100 casos.
- `scripts/check_gasometrias.js`: única mudança de código em 5 lotes — a regex de validação do id foi ajustada de `G-\d\d` para `G-\d{2,3}` para aceitar `G-100` (3 dígitos).
- Verificado: `npm run ci` verde, `check_gasometrias.js` com os 100 casos recalculados e cruzados contra o gabarito, `render_smoke.js` verde, Playwright confirmando os 100 reveals sem erro de console e as 8 categorias agregando corretamente (29+13+18+12+16+7+3+2 = 100).
- **Marco**: banco de gasometrias/bioquímica do `painel/index.html` completo em 100 casos, 100 diagnósticos diferentes, cobrindo CAD e seus mimics, toda a família de acidoses/alcaloses metabólicas e respiratórias (incluindo os 3 tipos de RTA, os 4 tipos clássicos de hipóxia tecidual, o trio hipertermia+rigidez+acidose lática, e a família de falência da "bomba" respiratória por 5+ mecanismos diferentes), toxicologia, endocrinologia, neuromuscular, pediatria, contextos cirúrgicos/iatrogênicos, erro pré-analítico puro e um controle negativo genuíno — todos sintéticos, de-identificados, com cada pH derivado por Henderson-Hasselbalch e cada afirmação do gabarito mecanicamente verificada pelos mesmos `core/abg_core.js`/`core/cad_core.js` do resto do repositório.
